# 公告实现与验证记录

已完成：

- 主题清单五种配置声明同步添加公告字段，支持嵌套及点分隔配置读取。
- 公告独立获取公开设置并定期同步，共用导航吸顶区域。
- 管理员 Markdown 编辑、预览、日期选择、固定四选一文字颜色、保存反馈和立即关闭。
- 导航栏和公告分别渲染为带间距的独立毛玻璃 island，公告不带外边框并保持导航栏同款模糊背景。
- 保存串行合并服务器配置，主题外观保存保留最新公告。
- 移动端限高、内部滚动、弹窗层级、三种语言及使用说明。

验证结果：

- TypeScript 和生产构建通过。
- 新增文件定向 ESLint 无错误；Context 同时导出 Provider 和 hook，存在一条 fast-refresh 建议性警告，沿用项目模式。
- 日期区间边界、时区等价、非法日历日期、空内容、关闭状态、危险 URL、配置格式检查通过。
- 主题清单字段及翻译键一致性检查通过。
- Playwright 使用本机模拟后端验证：390px 手机横幅吸顶、长内容滚动；修复并确认编辑弹窗 scrollWidth 与 clientWidth 均为 358px，无横向溢出。
- 验证 Markdown 预览、空内容拒绝保存、危险链接移除、保存成功、HTTP 500 保存失败反馈及重试。
- 验证主题配色保存保留公告、访客无管理入口、实例详情页横幅、无需刷新自动开始及自动结束。
- 检查桌面 1280px 深色模式截图。

未连接真实 Komari 后端发布公告。宿主管理后台的实际表单呈现需随主题上传后验收；本仓库通过已有主题清单机制提供字段。
